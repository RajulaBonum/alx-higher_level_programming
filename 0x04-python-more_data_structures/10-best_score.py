#!/usr/bin/python3
def best_score(a_dictionary):
    if a_dictionary:
        max_v = max(a_dictionary.values())
        for key in a_dictionary.items:
            if value == max_v:
                best_s = key
    else:
        return None
