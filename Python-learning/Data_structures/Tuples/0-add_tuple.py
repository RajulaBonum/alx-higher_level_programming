#!/usr/bin/python3
"""Add tuples"""
def add_tuple(tuple_a=(), tuple_b=()):
    c = ()
    d = ()
    for i in tuple_a and tuple_b:
        if i < 1:

        c = (tuple_a[i]+tuple_b[i])
        if i > 1
    return c
